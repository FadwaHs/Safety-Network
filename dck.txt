version: '3'
services:
  crud-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432
      POSTGRES_DB: locations
      POSTGRES_USER: ilisi
      POSTGRES_PASSWORD: ilisi

  business-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    depends_on:
      - crud-service

  socket-crud:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3002:3000"
    depends_on:
      - crud-service

  db:
    networks:
      - pg-network
    image: kartoza/postgis:13.0
    environment:
      POSTGRES_DB: locations
      POSTGRES_USER: ilisi
      POSTGRES_PASSWORD: ilisi
    ports:
      - "15432:5432"
    volumes:
      - /home/hassane/lab/pg-data:/var/lib/postgresql/data
networks:
  geonetwork:
  pg-network:
